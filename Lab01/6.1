import javax.swing.JOptionPane;

public class bai_6_1 {

    public static void main(String[] args) {
        // int option = JOptionPane.showConfirmDialog(null, "Do you want to change to
        // the first class ticket?");
        /*
         * int option = JOptionPane.showConfirmDialog(null,
         * "Do you want to change to the first class ticket?",
         * "Select an Option",
         * JOptionPane.YES_NO_OPTION);
         */
        Object[] options = { "I do", "I don't" };
        int n = JOptionPane.showOptionDialog(null,
                "Do you agree?",
                "Custom Option",
                JOptionPane.YES_NO_OPTION,
                JOptionPane.QUESTION_MESSAGE,
                null, // Không dùng icon tùy chỉnh
                options, // Mảng chứa tên các nút
                options[0]); // Nút mặc định khi nhấn Enter
        JOptionPane.showMessageDialog(null, "You've chosen: " + (n == JOptionPane.YES_OPTION ? "I do" : "I don't"));
        System.exit(0);
    }

}
